const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function test() { const products = await prisma.catalogProduct.findMany({ include: { productVariants: true } }); console.log(JSON.stringify(products.filter(p => p.options || p.productVariants.length > 0).map(p => ({ id: p.id, name: p.name, options: p.options, variants: p.productVariants })), null, 2)); } test();
