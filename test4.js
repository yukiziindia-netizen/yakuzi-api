const { NestFactory } = require('@nestjs/core'); const { AppModule } = require('./dist/app.module'); async function bootstrap() { const app = await NestFactory.createApplicationContext(AppModule); const productsService = app.get('ProductsService'); const adminService = app.get('AdminService'); const p = await adminService.getSuggestions(0, 1); const id = p.data[0].id; const master = await productsService.findOne(id); console.log(JSON.stringify(master, null, 2)); await app.close(); } bootstrap();
